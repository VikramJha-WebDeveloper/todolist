import styles from "./AppName.module.css";
let AppName = () => {
  return (
    <h1 className={`pt-4 head text-primary text-center ${styles.head}`}>
      ITEMS LIST
    </h1>
  );
};
export default AppName;
